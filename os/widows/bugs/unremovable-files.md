# Un-removable Files

- Files sometimes, especially within dev directories (e.g.: pnpm store), get stuck and cannot be deleted normally.
- Run cmd as admin (Start -> type 'cmd' -> Right click -> 'Run as administrator').
- Run `rmdir /s /q "path\to\parent\directory"`.
